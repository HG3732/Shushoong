<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kh.mclass.shushoong.member.model.repository.MemberRepository">
	<select id="login" parameterType="string" resultType="MemberEntity">
		SELECT USER_ID, USER_NAME, USER_PWD, USER_EMAIL, USER_GRAGE, USER_STATUS, JOIN_DATE, EMAIL_RECEIVE, MSG_RECEIVE FROM MEMBER WHERE USER_ID=#{userId} AND USER_PWD=#{userPwd};
	</select>
</mapper>
